import SwiftUI

@main // The @main make this the entry point of the program — the code the app starts running first.
struct ColorGridApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
